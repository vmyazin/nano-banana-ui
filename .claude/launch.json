{
  "configurations": [
    {
      "name": "WAN 3 reference-video smoke test",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --port 3217",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Universal prompt panel smoke test",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --port 3221",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Account cloud plan smoke test",
      "type": "node-terminal",
      "request": "launch",
      "command": "ACCOUNT_WORKER_PORT=8797 npm run dev -- --port 3097",
      "cwd": "${workspaceFolder}"
    }
  ]
}
